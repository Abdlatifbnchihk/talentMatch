## MODIFIED Requirements

### Requirement: Chat layout adjustment
The chat page SHALL use the updated design system colors and spacing.

#### Scenario: Slate palette
- **WHEN** chat renders
- **THEN** sidebar uses border-r border-slate-200 and white bg instead of gray variants

#### Scenario: Message styling
- **WHEN** messages render
- **THEN** assistant messages use bg-white and border-slate-100 instead of gray variants
